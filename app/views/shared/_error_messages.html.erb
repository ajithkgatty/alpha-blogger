<% if obj.errors.any? %>
	<div class="jumbotron">
		<h1 class="center">The form contains <%= pluralize(obj.errors.count, "error") %>.</h1>
	</div>
	<ul>
		<% obj.errors.full_messages.each do |msg| %>
		<li style="list-style: none"> <%= msg %></li>
		<% end %>
 	</ul>
	</div>
<% end %>